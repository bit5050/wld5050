/** Spring physics animations — client components only. */
export {
  animated,
  useSpring,
  useTrail,
  useTransition,
  config,
  to,
} from '@react-spring/web'

export type { SpringValue, AnimatedProps } from '@react-spring/web'
