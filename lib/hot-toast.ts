/**
 * Alternative toast API — react-hot-toast (leaner).
 * Use `lib/toast.ts` (Sonner) by default; import this only if you prefer hot-toast.
 */
export { default as hotToast, Toaster as HotToaster } from 'react-hot-toast'
