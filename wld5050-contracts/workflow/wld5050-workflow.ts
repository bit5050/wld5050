/**
 * Legacy entry point — replaced by ./main.ts (CRE SDK workflow with 30s cron).
 *
 * From wld5050-contracts/:
 *   cd workflow && bun install
 *   cd .. && cre workflow simulate workflow --target production-settings --non-interactive --trigger-index 0
 */
